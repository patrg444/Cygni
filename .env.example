# Database
# ⚠️ SECURITY: Generate strong passwords! Example: openssl rand -base64 32
DATABASE_URL=postgresql://postgres:✨CHANGEME_STRONG_PASSWORD✨@localhost:5432/cygni_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=✨CHANGEME_STRONG_PASSWORD✨
POSTGRES_DB=cygni_dev

# Redis
# ⚠️ SECURITY: Use a strong password for Redis too!
REDIS_URL=redis://:✨CHANGEME_REDIS_PASSWORD✨@localhost:6379
REDIS_PASSWORD=✨CHANGEME_REDIS_PASSWORD✨

# Auth
# ⚠️ SECURITY: Generate secrets with: openssl rand -hex 32
JWT_SECRET=✨CHANGEME_MIN_32_CHARS_USE_OPENSSL_RAND_HEX_32✨
SESSION_SECRET=✨CHANGEME_USE_OPENSSL_RAND_HEX_32✨

# MinIO (S3-compatible storage)
# ⚠️ SECURITY: Change these default credentials!
MINIO_ROOT_USER=✨CHANGEME_MINIO_USER✨
MINIO_ROOT_PASSWORD=✨CHANGEME_MIN_8_CHARS✨

# AWS (optional for local dev)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=✨CHANGEME_IF_USING_AWS✨
AWS_SECRET_ACCESS_KEY=✨CHANGEME_IF_USING_AWS✨

# Stripe (optional for local dev)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email (optional for local dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@cygni.dev

# Environment
NODE_ENV=development
PORT=3000

# Monitoring (optional)
SENTRY_DSN=
LOG_LEVEL=debug